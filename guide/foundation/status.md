# STATUS — Van Code (VS Code extension)

> _Generated with **GuideForge v1.2.0**._
>
> _Last updated with **GuideForge v1.16.0** on 2026-08-16._

## Frontier
- **Current frontier:** **M1–M5 executed; M6/01 is next up.** The reader has followed the guide end-to-end
  through `M5/09_verify.md`. **M1–M4 are ✅**; **M5 is ⏳** — its gate passed on every check but **check 5
  (per-language scoping), which is broken by the API and is being repaired in M6, not by re-doing M5**. See
  [`progress.md`](progress.md), which is the authority on which steps were run. **M6 (per-role color editing) and
  M7 (packaging) are drafted but not executed**, so everything from `M6/01_types-overrides.md` onwards is still
  free to change. Historic drafting state, for context: whole guide (M1→M7) **drafted + audited twice + fixes applied**. `/audit-guide` ran over all 7 milestones twice on 2026-08-09: the first pass → FAIL (2 structural blockers, 1 high suspect, 7 warnings, 5 minors), the confirmation re-audit → **PASS-WITH-WARNINGS** (0 blockers, 4 warnings, 8 minors, 2 open suspects), all of which are now fixed. **M6 and M7 have been audited** (they hadn't been when the milestone was drafted). The ladder grew from 5 to 7 milestones on 2026-08-09 (new M6 — per-role color editing; packaging split out into M7). M1–M5 were also audited twice on 2026-07-10 (PASS-WITH-WARNINGS, all findings fixed, re-audit 4/4 resolved). Mechanically clean as of the re-audit: 0 dead links, 0 dead `#anchor` deep-links, 0 nav defects, step-count headers and pinned versions consistent guide-wide. M6 and M7 are still ✅-free — a person must run each remaining Done-when gate (the M6 F5 flow + `vsce package`) by hand before either is marked verified.

## Source inputs
| Input file | Used for (stack / scope / decisions) | Provided on | Re-checked on |
|------------|--------------------------------------|-------------|---------------|
| User idea brief (in-prompt spec) | scope, features, milestone ladder seed, starter combos, 13 styles | 2026-07-10 | — |

## Milestone status
| Milestone | Status | Verified on | Notes |
|-----------|--------|-------------|-------|
| M1 — Scaffold + sidebar panel | ✅ verified | 2026-08-16 | **executed end-to-end and the `10_verify.md` gate passed by hand under F5** (reported 2026-08-16, ledger in [`progress.md`](progress.md)); drafted (9 steps + verify); **2026-07-13: audit-fix — M1/09 rendered as a complete file (doc-only, no behavior change)**; **2026-07-21: flagged for re-verify by the pedagogy-contract retrofit** |
| M2 — Non-destructive backbone | ✅ verified | 2026-08-16 | **executed end-to-end and the `06_verify.md` gate passed by hand under F5** (reported 2026-08-16, ledger in [`progress.md`](progress.md)); drafted (5 steps + verify); **2026-07-13: audit-fix — count-reconciliation note added to 06_verify (doc-only)**; **2026-07-21: flagged for re-verify by the pedagogy-contract retrofit**; **2026-08-07: report-issue — demo apply now writes 4 keys (status-bar debugging pair added); gate expects a 4-key block** |
| M3 — Color-formula engine (chrome) | ✅ verified | 2026-08-16 | **executed end-to-end and the `08_verify.md` gate passed by hand (engine `node -e` checks + F5)** (reported 2026-08-16, ledger in [`progress.md`](progress.md)); drafted (7 steps + verify); **2026-07-13: audit-fix — count-reconciliation note added to 08_verify (doc-only)**; **2026-07-21: flagged for re-verify by the pedagogy-contract retrofit**; **2026-08-07: report-issue sweep — gates no longer claim the status bar recolors under F5 (engine stays at 20 keys, D7)** |
| M4 — Preset gallery + panel UX (reality-check gate) | ✅ verified | 2026-08-16 | **executed end-to-end and the `05_verify.md` gate — including the ⭐ reality check — passed by hand under F5** (reported 2026-08-16, ledger in [`progress.md`](progress.md)); drafted (4 steps + verify; **2026-08-09: step 05 merged into 04 and `06_verify` renumbered to `05_verify` by the audit-fix pass, rule 4.4**); **2026-07-13: double-apply bug fixed in `main.js`**; **2026-07-21: flagged for re-verify by the pedagogy-contract retrofit**; **2026-08-07: report-issue sweep — 06_verify check 5 no longer lists the status bar as a live-recoloring surface** |
| M5 — Tokens and persistence | ⏳ executed, gate incomplete | — | **executed end-to-end under F5 on 2026-08-16; `09_verify.md` passed except check 5** (ledger in [`progress.md`](progress.md), which marks `03_apply-extend.md` and `09_verify.md` `[~]`). The per-language write throws — neither token setting is language-overridable ([feedback log, 2026-08-10](../feedback-log.md)) — so **M5 cannot go ✅ on this route**. By the reader's decision of 2026-08-16 the step is **not** re-done: the repair is delivered in **M6**, and M5 carries a pointer to it. Everything else in M5 (tokens + semantic applied globally, history, saved sets, export/import) is verified working. Drafted (8 steps + verify); **2026-07-13: double-apply bug fixed in final `main.js`**; **2026-07-21: flagged for re-verify by the pedagogy-contract retrofit**; **2026-08-09: packaging step moved out to M7, folder renamed `MILESTONE_5_tokens-and-persistence/`, steps renumbered 01–09, `package.json` checkpoint dropped (M5 no longer touches it)** |
| M6 — Per-role color editing | ⏳ needs re-verify (amended, never run) | — | **2026-08-16: amended — carries the corrections section that repairs M5's per-language mechanism ([D11](decision-log.md#d11--language-scoping-lives-inside-the-setting-value-and-a-scoped-apply-replaces-the-global-one)), plus a new gate check 10 and a canonical `src/theme/apply.ts` in the checkpoint; the gate is now 10 checks**; drafted 2026-08-09 (9 steps + verify); audited + fixed same day; **open suspect: the native `<input type="color">` dialog inside a sandboxed webview is unverified — the hex box is the documented fallback**; nothing executed — engine checks (`node -e`) and the 28-picker F5 flow are both pending. New file `src/engine/overrides.ts`; edits to `types.ts`, `generate.ts`, `sets.ts`, `ThemePanelProvider.ts`, `styles.css`, `main.js`. `src/theme/*` deliberately untouched |
| M7 — Packaging | ⏳ needs re-verify | — | **2026-08-16: amended — the M5 recap and the "`src/theme/` never changed" claim in 02_verify corrected for [D11](decision-log.md#d11--language-scoping-lives-inside-the-setting-value-and-a-scoped-apply-replaces-the-global-one)**; drafted 2026-08-09 (1 step + verify) from M5's former step 09; audited + fixed same day; `vsce package` + the install-in-an-ordinary-window check (which closes the D7 status-bar exception) both pending |

<!-- Status key: ✅ verified (Done-when passed by hand) · ⏳ in progress · ❌ not started -->

> **Verification honesty note:** this guide builds a VS Code extension whose "done" is observed in a running
> Extension Development Host (F5) — a GUI + editor-host process that is **not** headlessly simulatable here. The
> `src/engine/` color math **is** Node-runnable and unit-checkable without VS Code; the API/webview/apply
> behavior must be verified by a person pressing F5. When a milestone is marked ✅, state which parts were
> machine-verified (engine) vs. observed-in-editor (everything touching `vscode`).

## Drift log
| Date | Where | Guide said | Reality is | Action taken |
|------|-------|-----------|-----------|--------------|
| 2026-08-16 | M5/03 + M5/09 + M5/00 (behind the frontier), M6/01 + M6/10 + M6/00, M7/00 + M7/02, D2, `stack.md`, `PLAN.md`, `README.md`, `glossary.md` | Per-language token scoping is done by obtaining the config with `{ languageId }` and passing `overrideInLanguage: true`, which writes the value into a `"[typescript]"` block that `editor.*` settings accept and `workbench.*` settings don't | A language override is allowed only on settings declared **resource-language**-scoped. Both `editor.tokenColorCustomizations` and `editor.semanticTokenColorCustomizations` are application-scoped theme-customization settings, so the write is rejected — `CodeExpectedError: … is not a resource language setting` — and VS Code closed the request to change that as out of scope ([vscode#124997](https://github.com/microsoft/vscode/issues/124997), [vscode#66729](https://github.com/microsoft/vscode/issues/66729), verified 2026-08-16). The language belongs **inside the value**: semantic rule keys take the `(:tokenLanguage)?` suffix ([Semantic Highlight Guide](https://code.visualstudio.com/api/language-extensions/semantic-highlight-guide)), and TextMate needs `textMateRules` with descendant scopes like `source.ts comment` ([TextMate manual](https://macromates.com/manual/en/scope_selectors)) because the seven named groups cannot carry a language | `/amend-guide`. **M5 was not rewritten** — it is executed: steps 00, 03 and 09 got a superseded banner and nothing else. The repair is a *Before you continue — corrections* section at the top of **M6/01**, rewriting `applyTokens`/`applySemantic` and adding a `languageId → TextMate root` map; `applyChrome`, `applyTheme`, the provider and the webview are untouched, so the `apply` message contract is unchanged. M6/10 gained gate check 10 and a canonical `src/theme/apply.ts` copy; M6/00, M7/00 and M7/02 recaps corrected. [D2](decision-log.md#d2--per-language-scoping-is-tokens-only) amended in place (its tokens-only conclusion stands, its mechanism doesn't); new [D11](decision-log.md#d11--language-scoping-lives-inside-the-setting-value-and-a-scoped-apply-replaces-the-global-one) records the *scoped apply replaces the global value* trade-off, taken so `src/theme/` stays a whole-value replace and M2's snapshot/Revert backbone never moves. **M6 and M7 need re-verification.** |
| 2026-08-07 | M2/05 + M2/06 + M2/00 (and, as siblings, M3/06, M3/07, M3/08, M3/00, M4/06) | The demo apply wrote `statusBar.background` / `statusBar.foreground` only, and every gate from M2 to M4 told the reader to confirm a live apply by **watching the EDH's status bar** | The Extension Development Host is a window with an active debug session, so VS Code paints its status bar from `statusBar.debuggingBackground` / `statusBar.debuggingForeground`, which **override** the ordinary pair ([Theme Color reference](https://code.visualstudio.com/api/references/theme-color#status-bar-colors), re-verified 2026-08-07). The write landed correctly; the color was simply not painted until the session stopped | M2's demo now applies **four** keys (both pairs, same hexes) — step 05 code + the 06 file checkpoint + the 4-key `settings.json` block in the 06 gate and the 00 aggregated gate. Added a "New concept" note on debugging-color precedence (M2/05), a glossary entry, a rule-5.1 failure note at M2/05 + a troubleshooting row at M2/06, and a precedence caveat on M2/02's "the write applies live". Swept M3–M5: because `applyChrome` **replaces** the whole object, M3's generated 20-key palette drops the debugging keys, so the status bar is masked again from M3 on — the gates that claimed "status bar a bright cyan" (M3/08) / listed it among live-recoloring surfaces (M3/07, M3/00, M4/06) were **false under F5** and are rewritten to state the exception, with a failure note in M3/06 and a troubleshooting row in M3/08. M5/10 closes the loop (installing the `.vsix` recolors it normally). Engine left at 20 keys by [D7](decision-log.md#d7--m2s-demo-sets-the-status-bar-debugging-colors-the-m3-engine-map-does-not). **M2, M3, M4, M5 need F5 re-verification.** |
| 2026-08-04 | M1/05 (+ M1/10, M5/10 full-manifest listings) | The `contributes.views` entry carried only `id`, `name`, `type` | VS Code's bundled manifest schema flags that object with `Missing property "icon".` (the docs call `icon` optional; the editor's schema does not) | Added `"icon": "media/icon.svg"` to the view entry in all three listings, plus a concept note explaining why `icon` appears on both the container and the view, and an "If it breaks" guard for the squiggle. Reported by a reader following M1/05. **M1 + M5 need F5 re-verification.** |
| 2026-08-04 | Whole guide (all milestones + foundation) | Extension named `Live Recolor` / `live-recolor` / `liveRecolor` | The extension is **Van Code**, identifier **`van-code`** | Renamed throughout: display names, package `name`, project folder, view container id `vanCode`, view id `vanCode.panel`, `globalState` key `vanCode.savedSets`, `Van Code: imported …` toast, `Hello World from Van Code!`, `van-code-0.0.1.vsix`, and the `description` aligned to `Recolor the whole editor from a sidebar panel`. Doc-only; no behavior change. |
| 2026-07-13 | Audit-fix pass (M1, M2, M3, M4, M5 + conventions) | Assorted minor/low audit findings | — | Non-major audit fixes: noted the `resolveWebviewView` `_context`/`_token` param drop (M4/04 + M5/06); removed the never-used "command ids" from conventions.md load-bearing names + casing example (the extension contributes no commands); reconciled overview-vs-walkthrough check counts (M2/06, M3/08, M4/06 verify); rendered M1/09's `ThemePanelProvider.ts` as a complete file (was the lone partial snippet). **M1-M5 need F5 re-verification (never mark verified without a Done-when run by hand).** _(+ 2026-07-13 re-audit follow-up: first-step nav `prev → —` applied across every milestone's `01_*.md` — cosmetic nav-line only, no Done-when impact.)_ |
| 2026-07-13 | M4/03 + M4/06 verify, M5/07 + M5/10 verify (`media/webview/main.js`) | `render()` ended with `apply()` **and** every chip handler called `render(); apply();` | That fires two identical `apply` messages per pick → the provider snapshots twice → the first **Revert** looks inert (contradicts the M4/06 "one snapshot per apply" gate) | Removed the `apply()` at the end of `render()` (now pure draw); apply once in each click handler plus one apply after the first `render()` in the `init` handler. Reconciled the M4/06 gate + M5/10 Revert check and added an R10 guard in M4/03. |

## Session log
- 2026-08-16 — **`/amend-guide`: per-language token scoping re-cast to live inside the setting value.** Driven by
  the 2026-08-10 reader report, whose facts were re-verified against the official docs the same day: the
  `overrideInLanguage` route M5/03 taught cannot work on either token setting, and VS Code has closed the request
  to allow it. Applied against a frontier of `M6/01_types-overrides.md`. **Behind the frontier: banner only** —
  M5/00, M5/03 and M5/09 each got a superseded banner on line 3 and were not otherwise edited, because the reader
  has already executed them. **The repair lives ahead of the frontier**, as a *Before you continue — corrections*
  section at the top of M6/01: two lookup tables (`TEXTMATE_ROOT`, `TOKEN_SCOPES`), rewritten `applyTokens` and
  `applySemantic`, `applyChrome`/`applyTheme`/the provider/the webview all untouched. M6/10 gained gate check 10
  and a canonical `src/theme/apply.ts`; M6/00's aggregated gate, M7/00's recap and M7/02's retrospective corrected.
  D2 amended in place, D11 added, plus `stack.md`, `PLAN.md`, `README.md` (Updates) and three glossary terms.
  **M6 and M7 are ⏳ and must be re-verified; M5 stays ⏳ until the reader applies the correction** — its two
  invalidated rows are `[!]` in [`progress.md`](progress.md). **Reported, not fixed** (they are defects, not a
  change of intent, so they belong to `/report-issue`): M5/04's aside still describes `"[languageId]"` blocks, and
  the 2026-08-10 entries for M5/06 (unhandled rejection) and M5/07 (free-text language box) are still open.
- 2026-08-16 — **`/mark-progress`: the execution ledger exists, and M1–M5 are executed.** Created
  [`foundation/progress.md`](progress.md) from scratch (this guide predates the ledger) with one row per step
  file on disk — 10 + 6 + 8 + 5 + 9 + 10 + 2 = 50 rows across M1→M7. The reader reports every step of **M1
  through M5 executed** by hand under F5. **M1–M4 gates passed → ✅ (verified 2026-08-16)**, 29 rows `[x]`.
  **M5 is `⏳`, not ✅:** on re-checking, `09_verify.md` **check 5 (per-language scoping) still fails** — M5/03's
  `overrideInLanguage` route throws, exactly as the 2026-08-10 feedback-log entry predicted — so
  `03_apply-extend.md` and `09_verify.md` are marked `[~]` with what is outstanding, and the other 7 M5 rows are
  `[x]`. The reader's decision, same day: **do not re-do M5 — deliver the repair in M6**, with M5 carrying a
  pointer to it. **M6 and M7 stay untouched and unticked** — the frontier is `M6/01_types-overrides.md`, and
  that is the boundary any amendment must respect. Marking only — no step file, overview or foundation doc
  other than these two was edited.
- 2026-07-10 — Plan approved; scaffolded skeleton (README, 6 foundation docs, 5 milestone overview placeholders, feedback log, token ledger). No step content drafted.
- 2026-07-10 — Drafted M1 (Scaffold + sidebar panel): 10 atomic steps + verify, full file checkpoint. Built against stack.md's same-day-verified API facts. **Not executed** — GUI/F5 flow is not headlessly simulatable here; awaiting F5 verification by hand.
- 2026-07-10 — Drafted M2–M5 in one pass (cadence change) via 4 parallel subagents against a locked CODE_SPEC (scratchpad) so code can't drift. M2 (5+verify), M3 (7+verify), M4 (5+verify, reality-check gate), M5 (9+verify). All code verbatim from the spec; subagents reported no logic deviations (M3 dropped an unused `rotate` import from `profiles.ts` — to reconcile vs M4's copy in the audit). Dead-link check clean. **Audit paused** pending user go. Nothing executed/packaged — awaiting F5 + `vsce package` by hand.
- 2026-07-10 — Ran `/audit-guide` over the whole guide: **PASS-WITH-WARNINGS**. Structure/format/link-integrity/scope/code-fidelity essentially spotless across 45 files. Fixed all findings: (1) compile blocker — `applyTheme(set)` was a type error (`SavedSet` ≠ `ThemeResult`); widened `applyTheme` to `Pick<ThemeResult,'chrome'|'tokens'|'semantic'>` and typed `SavedSet.{chrome,tokens,semantic}` as `ChromeColors`/`TokenColors`/`SemanticColors` (also fixed a latent error in the `save` handler) — in M5/03, M5/05, M5/10 + spec; (2) M4 `profiles.ts` unused `rotate` import removed (M4/01, M4/06) so M3→M4 is a clean no-op; (3) pedagogy: glossed "gamma-corrected" (M3/01), tightened "~20 keys" → exact "20" (M3 gates). Re-verified: no stale patterns; the three imported color types are exported by M5 types.ts. Still nothing executed — awaiting F5 + `vsce package` by hand.
- 2026-07-10 — Re-ran `/audit-guide` after the fixes: **PASS-WITH-WARNINGS**, 4/4 prior findings RESOLVED, type chain compiles, 0 structural blockers, 0 cross-milestone drift, 0 pedagogy violations. Only item was this frontier line being stale ("Audit not yet run") — now corrected. Guide is ready for a person to follow. Still nothing executed.
- 2026-07-13 — **Report-issue: double-apply bug.** The webview `render()` ended with `apply()` while every chip handler also called `apply()`, so each pick posted two `apply` messages → two history snapshots → the first Revert looked inert (broke the M4/06 "one snapshot per apply" gate). Fix: `render()` is now pure draw; apply once per click handler + once from `init`. Applied to M4/03, M5/07 and both verify checkpoints; gate/Revert wording reconciled; R10 guard added. M4/M5 flagged ⚠ re-verify. Nothing executed — awaiting F5 by hand.
- 2026-07-13 — Confirmation re-audit of the double-apply fix: **PASS** (no bare `apply()` in `render()`, init applies once in all 4 files, fences balanced, no new dead links). Still awaiting F5 by hand.
- 2026-08-07 — **Report-issue: the demo color was invisible under F5.** A reader following M2/05 saw `settings.json` gain the block but the EDH's status bar stay orange — the crimson appeared only after <kbd>Shift</kbd>+<kbd>F5</kbd>. Root cause: the EDH is a debugged window, so `statusBar.debugging*` overrides `statusBar.background`; the guide had picked, as its single observable proof, the one surface VS Code overrides in the exact environment it told the reader to watch. Fix: M2's demo applies both pairs (4 keys) + a precedence concept note, glossary entry, failure note and troubleshooting row. Sweep found the same class in M3/06–08, M3/00 and M4/06 (the generated 20-key palette *replaces* M2's keys, re-masking the bar) — those gates now state the exception rather than claim a recolor; M5/10 closes the loop after `.vsix` install. Engine deliberately left at 20 keys ([D7](decision-log.md#d7--m2s-demo-sets-the-status-bar-debugging-colors-the-m3-engine-map-does-not)). M2–M5 flagged ⏳ re-verify. Nothing executed — F5 by hand still pending.
- 2026-07-21 — **Retrofit to the GuideForge v1.2.0 pedagogy contract.** Foundation-docs pass (Part 1): stamped provenance on README/status, converted `foundation/glossary.md` from bullets to `### <term>` concept headings so `#slug` deep-links resolve, and removed the `acquireVsCodeApi()` entry (it's a function → inline code comment, not a glossary concept; per rule 1.1d). Step files (code-under-instruction 4.2, existing-file edits as fragments 4.3, "Before you start" starting-state notes 7.1, and the `acquireVsCodeApi()` inline comment) are handled by a separate step-files pass. All milestones M1–M5 flagged ⏳ needs re-verify; nothing executed — F5 hand-verification still pending.
- 2026-08-09 — **Audit-fix pass over the whole guide (`/audit-guide` M1→M7 → FAIL, all findings fixed).** Blockers: (1) **rule 4.4 — three steps ended on a red build**, each telling the reader an error was "expected" so a real mistake of theirs would hide inside it. M2/04 now also adds the provider constructor (call site + signature in one step); M4/04 absorbed M4/05's one-line `extension.ts` change (**`05_wire-extension.md` deleted, `06_verify.md` → `05_verify.md`, M4 is now 4 steps + verify**); M5/01 became purely additive and the `ThemeResult` widening moved into M5/02 beside the `generate()` rewrite that satisfies it. (2) **Dead capability** — M6's exported `countOverrides` was never called and its load-bearing note falsely claimed three importers; removed, the webview's `countPinned` is the single counter. Warnings: M6/04 pointed at the wrong dev tools (host, not webview); M6's load-bearing table wrongly claimed unknown roles are dropped by the merge; the `start:` nav segment was missing from all 7 overviews; 13 third-person "the reader" occurrences in M6 (two inside code the reader types) rewritten to "you"; M6's gate now restates the D7 status-bar exception; `stack.md` said packaging was M5; `status.md` mis-counted M1's steps. Minors: M3/06 prose still said `p.accent1` (D8 leftover), M7/01's `.vsix` check was Windows-only, M1/10 now explains why `package.json` is reproduced whole, PLAN's "awaiting approval" header, M6's pre-first-apply footer. **Open suspect, not fixable from here:** whether a sandboxed VS Code webview surfaces the OS color dialog for `<input type="color">` — M6/05, M6/06 and M6/10 now document the hex box as the full-strength fallback, and `/review-before-follow` against a real EDH should settle it. Nothing executed.
- 2026-08-09 — **Ladder extended 5 → 7: new M6 (per-role color editing), packaging split into M7.** Requested feature: the reader must be able to choose every customizable color individually. Scoped to the **28 roles the engine emits** (8 `Palette` + 7 `TokenColors` + 13 semantic types) rather than VS Code's ~600 theme keys ([D9](decision-log.md#d9--overrides-are-role-level-not-vs-code-key-level)), and implemented as a **sparse override map layered over the formula** inside `generate()` — three merge lines, one new pure module (`src/engine/overrides.ts`), **zero changes to `src/theme/`**, so M2's snapshot backbone and the one write path are untouched. Edits commit on `change`, never `input`, so one deliberate edit is still one snapshot ([D10](decision-log.md#d10--role-edits-commit-on-change-not-on-input)) — the same failure class as the 2026-07-13 double-apply bug. Because the 28 roles include token/semantic colors that only exist from M5 on, the picker milestone had to follow M5: M5 kept tokens + persistence (renamed folder `MILESTONE_5_tokens-and-persistence/`, steps renumbered 01–09), M6 is the new picker milestone (9 steps + verify), M7 is packaging (M5's old step 09 + a new install/verify step that closes the D7 status-bar exception). Foundation reconciled: PLAN ladder + sittings + folder tree + the settings-JSON scope boundary, README objective/decisions/Updates, glossary (*role override*, *effective color*), decision-log (D9, D10). **Nothing executed** — M6's `node -e` engine checks and every F5 gate are pending, and **M6/M7 have not been through `/audit-guide`**.
- 2026-08-09 — **Confirmation re-audit of the whole guide (`/audit-guide` M1→M7 → PASS-WITH-WARNINGS), all findings fixed.** Mechanical checks came back clean (0 dead links, 0 dead `#anchor` deep-links across 48, 0 nav defects, step counts + pinned versions consistent), so every finding was a content one. **Two of the previous pass's claimed fixes had not actually landed** and are now applied: (a) the M6 third-person→"you" voice rewrite — 11 occurrences survived, including *both* of the ones inside code the reader types (`// The reader's hand-picked colors…` in M6/01 and its M6/10 checkpoint copy); (b) the D8 leftover in M3/06, where `p.accent1` was fixed but `p.surfaceAlt` was not. Other fixes: **M4's handoff and verify still routed packaging to M5** (three places, incl. a Done-when quoting `vsce package`) → repointed to M7; **M6/02's Done-when said "three exported functions"** where the file has two (the `countOverrides` removal again) → corrected; **two dead capabilities given honest markers** — `ThemeHistory.depth` (never called anywhere; now flagged `[unused]` in all three checkpoint copies with the reason) and `color.ts`'s `rotate` (first consumed in M5/02; now carries an `[M5]` marker, and M3/01's *Why* no longer credits it to Nature/Warm-Sepia, which both use `setHue`); **`conventions.md`'s "no single/two-letter locals" rule scoped to `src/engine/*`** with the adapter/webview carve-out written down and linked to [D8](decision-log.md#d8--engine-code-spells-its-locals-out-the-terse-math-names-are-gone) (it read as absolute, so `wb`/`ed`/`m`/`s`/`el` in the code the reader types were all technically violations); **D7's two live pointers to the deleted `M5/10`** repointed to M7/02 and the current file copies; **`renderPreview()` de-drifted** so the M4, M5 and M6 checkpoints are byte-identical, as M5/07 claims; M5/08 now actually *performs* the re-import its idempotency Done-when asserts; M3/01 + M3/03's "used unchanged through M5" captions corrected; PLAN's stale "nothing is scaffolded yet" header, its command-id load-bearing entry (the extension contributes none) and its `[Milestone overview]` nav template reconciled with the shipped guide. **Doc-only — no code or behaviour changed**, so no new re-verification burden beyond the F5 passes already pending. **Two open suspects remain, both needing a real EDH** (`/review-before-follow`): the `<input type="color">` OS dialog inside a sandboxed webview (carried over; the hex box is the documented full-strength fallback), and whether `editor.semanticTokenColorCustomizations` is genuinely language-overridable — M5/09 check 5 asserts a `"[typescript]"` block holds **both** token settings, and `stack.md`'s 2026-07-10 check covers `editor.*` generically rather than that setting by name.
- 2026-08-08 — **Naming pass over the pure engine (doc-only).** Renamed every terse local in the `src/engine/*` code blocks across M3/01, M3/03, M3/04, M3/05, M3/06, M3/08, M4/01, M4/06, M5/02, M5/10 (`p`→`palette`, `c`→`combo`, `d`→`chroma`, `la`/`lb`→`luminanceA`/`luminanceB`, `hi`/`lo`→`brighter`/`darker`, `to2`→`toHexPair`, `_c`→`_combo`, …), named the two intermediates the math has no word for (`maxChromaAtThisLightness`, `lightnessOffset`), and replaced `hslToHex`'s `if (h < 60) … else` ladder with a labelled `switch (Math.floor(hue / 60))`. Prose, `node -e` one-liners and troubleshooting rows updated to match; rule codified in [conventions.md](conventions.md#naming) ([D8](decision-log.md#d8--engine-code-spells-its-locals-out-the-terse-math-names-are-gone)); `chroma` added to M3/01's step glossary. **No behavior change** — verified byte-identical output vs. the pre-rename engine across all 85 combo × profile themes and a 17,760-color × 13-operation sweep, compiling clean under `--strict`. Adapter/webview code (`main.js`, `ThemePanelProvider.ts`) deliberately untouched — they keep their terse locals for now. No new re-verification burden beyond the F5 passes already pending.
